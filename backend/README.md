# AlgoGym Backend

FastAPI service for local-first algorithm training data, document ingestion, RAG orchestration, and practice tracking.

## Development

```bash
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload
```
